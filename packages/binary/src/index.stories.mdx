import { Story } from "@ladle/react";
import { DetailedBitString } from "./Detailed";

# What is this?

This part will be rendered as a separate `Readme` story.

<Story name="example">
  <DetailedBitString value={0b10101010} length={8} />
  <p>As you can see, <DetailedBitString value={0b10101010} length={8} /> is an inline element.</p>
</Story>
