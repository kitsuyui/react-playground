# @kitsuyui/react-binary

import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as DetailedStories from './Detailed.stories'
import { DetailedBitString } from "../";

<Meta title="Visualization Primitives/Binary/Overview" />
<Canvas of={DetailedStories.Default} />

As you can see, <DetailedBitString value={0b10101010} length={8} /> is an inline element.

`Bits` is the structural primitive.
`SimpleBitString` and `DetailedBitString` are concrete visualizations layered on top of that primitive.

## Installation

## Usage
