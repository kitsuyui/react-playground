# @kitsuyui/react-hello

import { Canvas, Meta } from '@storybook/addon-docs/blocks'

import * as HelloStories from './Hello.stories'

<Meta title="Semantic Wrappers/Hello/Overview" />
<Canvas of={HelloStories.Default} />

## Role

This package is a minimal semantic wrapper example.

## Layers

- `Hello` is intentionally simple and exists mainly as a reference for the package shape.

## Example

```tsx
import { Hello } from '@kitsuyui/react-hello'

function App() {
  return <Hello />;
}
```
