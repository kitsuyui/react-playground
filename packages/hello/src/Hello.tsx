import type React from 'react'

/**
 * A simple Hello World component for React.
 * @returns A JSX element that displays "Hello, world!".
 */
export const Hello = (): React.JSX.Element => {
  return <span>Hello, world!</span>
}
